/**
 * Atome dialog — pattern modulaire Banda :
 *   dialog/BandaDialog.tsx (base <dialog>) + models/ (un modèle = une intention) + index.
 */
export { BandaDialog, type BandaDialogProps, type DialogSize } from './BandaDialog';
export { AlertDialog, type AlertDialogProps } from './models/alert-dialog';
